# auv_propulsion

Final architecture placeholder for thruster allocation and hardware abstraction.

The allocator will map a desired wrench to the configured thruster layout. Simulation and real ESC/driver backends must consume the same abstract command contract.
