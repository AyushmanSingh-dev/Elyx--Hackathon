# Model weights

Place the trained YOLO weights here as `gate_detector_v2.pt`.

The binary model is intentionally not committed by this migration because it is not present in the available workspace. `gate_detector_node` resolves the configured model path relative to the `auv_vision` package share directory.
