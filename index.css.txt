/* index.css */
/* Import Tailwind CSS directives */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Custom fonts or global styles can go here */
/* The Inter font is loaded via CDN in App.js for simplicity in this environment */

/* Basic spinner style for loading indicators */
.spinner {
  border: 4px solid rgba(0, 0, 0, 0.1);
  border-left-color: #6366f1; /* Indigo 500 */
  border-radius: 50%;
  width: 40px;
  height: 40px;
  animation: spin 1s linear infinite;
  margin: 0 auto;
}
@keyframes spin {
  to { transform: rotate(360deg); }
}

/* Ensure the body font-family is applied */
body {
  font-family: 'Inter', sans-serif;
}